This directory is reserved for server-rendered Jinja2 templates. The initial
scaffold intentionally exposes only `/healthz`; application pages will be
added after authentication and synchronization behavior are designed.
